<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- 필수 권한 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GPS"
        android:usesCleartextTraffic="true"
        android:vmSafeMode="false"
        tools:targetApi="31">
        <activity
            android:name=".activities.MyGroupsActivity"
            android:exported="false" />
        <!-- 네이버 지도 API 키 -->
        <meta-data
            android:name="com.naver.maps.map.NCP_KEY_ID"
            android:value="iaxrf6bmtc" /> <!-- 앱 시작 액티비티 -->
        <activity
            android:name=".activities.Register_Login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- 네이버 지도 관련 액티비티 -->
        <activity
            android:name=".activities.MapsActivity"
            android:exported="false" /> <!-- 기타 화면들 -->
        <activity android:name=".activities.Register_Login.RegisterActivity" />
        <activity android:name=".activities.Register_Login.FindIdActivity" />
        <activity android:name=".activities.Register_Login.FindPwActivity" />

        <activity android:name=".activities.ChatRoomActivity"/>

        <activity
            android:name=".activities.Register_Login.ResetPasswordActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="reset-password"
                    android:scheme="app" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.Friend.FriendsActivity"
            android:exported="false" />

        <activity android:name=".activities.CreateGroupActivity" />
        <activity android:name=".activities.SettingsActivity" />
        <activity
            android:name=".activities.GroupSharingSettingsActivity"
            android:exported="false"
            android:label="위치 공유 설정" />

    </application>

</manifest>