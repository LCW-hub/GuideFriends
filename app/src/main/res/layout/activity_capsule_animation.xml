<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/nav_header_background">

    <!-- 가챠 머신 전체 컨테이너 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="20dp">

        <!-- 가챠 머신 본체 -->
        <FrameLayout
            android:layout_width="320dp"
            android:layout_height="400dp"
            android:background="@drawable/bg_card_round"
            android:elevation="8dp"
            android:layout_marginBottom="20dp">

            <!-- 머신 상단 (캡슐 저장소) -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="120dp"
                android:orientation="vertical"
                android:gravity="center"
                android:background="@drawable/bg_card_round"
                android:layout_margin="16dp">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="🎰"
                    android:textSize="32sp"
                    android:layout_marginBottom="8dp"/>

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="GACHA MACHINE"
                    android:textSize="14sp"
                    android:textStyle="bold"
                    android:textColor="@color/colorPrimary"/>

            </LinearLayout>

            <!-- 캡슐 투입구 -->
            <FrameLayout
                android:layout_width="80dp"
                android:layout_height="80dp"
                android:layout_gravity="center_horizontal|top"
                android:layout_marginTop="140dp"
                android:background="@drawable/bg_card_round"
                android:elevation="4dp">

                <ImageView
                    android:id="@+id/iv_capsule_drop"
                    android:layout_width="60dp"
                    android:layout_height="60dp"
                    android:layout_gravity="center"
                    android:src="@drawable/ic_star"
                    android:contentDescription="떨어지는 캡슐"
                    android:visibility="gone"/>

            </FrameLayout>

            <!-- 캡슐 수집구 -->
            <FrameLayout
                android:layout_width="120dp"
                android:layout_height="120dp"
                android:layout_gravity="center_horizontal|bottom"
                android:layout_marginBottom="20dp"
                android:background="@drawable/bg_card_round"
                android:elevation="6dp">

                <ImageView
                    android:id="@+id/iv_capsule"
                    android:layout_width="80dp"
                    android:layout_height="80dp"
                    android:layout_gravity="center"
                    android:src="@drawable/ic_star"
                    android:contentDescription="뽑힌 캡슐"/>

                <!-- 캡슐 주변 반짝이는 효과 -->
                <ImageView
                    android:id="@+id/iv_sparkle1"
                    android:layout_width="16dp"
                    android:layout_height="16dp"
                    android:layout_gravity="top|start"
                    android:layout_margin="12dp"
                    android:src="@drawable/ic_star"
                    android:alpha="0.7"
                    android:visibility="gone"/>

                <ImageView
                    android:id="@+id/iv_sparkle2"
                    android:layout_width="14dp"
                    android:layout_height="14dp"
                    android:layout_gravity="top|end"
                    android:layout_margin="16dp"
                    android:src="@drawable/ic_star"
                    android:alpha="0.5"
                    android:visibility="gone"/>

                <ImageView
                    android:id="@+id/iv_sparkle3"
                    android:layout_width="15dp"
                    android:layout_height="15dp"
                    android:layout_gravity="bottom|start"
                    android:layout_margin="14dp"
                    android:src="@drawable/ic_star"
                    android:alpha="0.6"
                    android:visibility="gone"/>

            </FrameLayout>

        </FrameLayout>

        <!-- 로딩 텍스트 -->
        <TextView
            android:id="@+id/tv_loading"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="캡슐을 뽑는 중..."
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@color/colorPrimary"
            android:layout_marginTop="20dp"/>

        <!-- 진행 바 -->
        <ProgressBar
            android:id="@+id/progress_bar"
            android:layout_width="200dp"
            android:layout_height="4dp"
            android:layout_marginTop="16dp"
            android:progressTint="@color/colorPrimary"
            style="?android:attr/progressBarStyleHorizontal"/>

        <!-- 캡슐 까기 버튼 (나중에 표시) -->
        <Button
            android:id="@+id/btn_open_capsule"
            android:layout_width="200dp"
            android:layout_height="48dp"
            android:text="캡슐 까기 🎁"
            android:textSize="16sp"
            android:textStyle="bold"
            android:background="@drawable/button_gradient"
            android:textColor="@android:color/white"
            android:elevation="4dp"
            android:layout_marginTop="20dp"
            android:visibility="gone"/>

    </LinearLayout>

</FrameLayout>


